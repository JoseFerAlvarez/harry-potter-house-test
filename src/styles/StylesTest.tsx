import styled from "styled-components"

const TestContainer = styled.div`
    display: flex;
    flex-direction: column;
    margin: auto;
    gap: 30px;
    max-width: 700px;
    margin-bottom: 100px;
`;

const UserContainer = styled.div`
    display: flex;
    justify-content: flex-end;
`;

export { TestContainer, UserContainer }
